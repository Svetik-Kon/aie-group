# EDA-отчёт

Исходный файл: `example.csv`

Строк: **36**, столбцов: **14**

## Параметры отчёта

- Максимум гистограмм (числовых колонок): 6
- Top-k категорий для категориальных признаков: 5
- Порог доли пропусков для проблемных колонок: 20%

## Качество данных (эвристики)

- Оценка качества: **0.54**
- Макс. доля пропусков по колонке: **5.56%**
- Слишком мало строк: **True**
- Слишком много колонок: **False**
- Слишком много пропусков: **False**
- Есть константные колонки: **False**
- Есть high-cardinality категориальные: **False**
- Есть дубликаты id-полей: **True**

- ID с дубликатами: `user_id`

## Колонки

См. файл `summary.csv`.

## Пропуски

См. файл `missing.csv` и `missing_matrix.png`.

### Колонки с долей пропусков ≥ 20%

Таких колонок нет.

## Корреляция числовых признаков

См. `correlation.csv` и `correlation_heatmap.png`.

## Категориальные признаки

См. файлы в папке `top_categories/`.

## Гистограммы числовых колонок

См. файлы `hist_*.png` (не более 6 числовых колонок).
